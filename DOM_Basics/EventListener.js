// addEventListener()

//Syntax: element.addEventListener(event, function, useCapture)

// useCapture -> which event to run first (defaults to false)


// removeEventListener()
//  -> Can only work on named functions